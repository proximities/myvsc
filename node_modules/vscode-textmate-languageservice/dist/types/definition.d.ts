import * as vscode from 'vscode';
import type { ConfigData } from './config';
import type { OutlineService } from './services/outline';
export declare class TextmateDefinitionProvider implements vscode.DefinitionProvider {
    private _config;
    private _outlineService;
    constructor(_config: ConfigData, _outlineService: OutlineService);
    provideDefinition(document: vscode.TextDocument, position: vscode.Position): Promise<vscode.Location[]>;
    private getNestedPosition;
    private getComponentGlob;
    private searchFiles;
}
