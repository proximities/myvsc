import type { LiteTextDocument } from '../services/document';
export interface ServiceInterface<T> {
    fetch(document: LiteTextDocument): Promise<T>;
    parse(document: LiteTextDocument): Promise<T>;
}
export declare abstract class ServiceBase<T> {
    private _cache;
    private _integrity;
    fetch(document: LiteTextDocument | string): Promise<T>;
    abstract parse(document: LiteTextDocument | string): Promise<T>;
}
