import * as vscode from 'vscode';
import type { OutlineService } from './services/outline';
import type { LiteTextDocument } from './services/document';
export declare class TextmateDocumentSymbolProvider implements vscode.DocumentSymbolProvider {
    private _outlineService;
    constructor(_outlineService: OutlineService);
    provideDocumentSymbolInformation(document: LiteTextDocument): Promise<vscode.SymbolInformation[]>;
    provideDocumentSymbols(document: LiteTextDocument): Promise<vscode.DocumentSymbol[]>;
    private traverseAndCopy;
    private toSymbolInformation;
    private toDocumentSymbol;
}
