import type * as vscode from 'vscode';
import { Disposable } from './util/dispose';
import type { TextmateDocumentSymbolProvider } from './document-symbol';
import type { DocumentService } from './services/document';
export declare class TextmateWorkspaceSymbolProvider extends Disposable implements vscode.WorkspaceSymbolProvider {
    private _documentService;
    private _documentSymbols;
    private _symbolCache;
    private _symbolCachePopulated;
    constructor(_documentService: DocumentService, _documentSymbols: TextmateDocumentSymbolProvider);
    provideWorkspaceSymbols(query: string): Promise<vscode.SymbolInformation[]>;
    private populateSymbolCache;
    private getSymbols;
    private onDidChangeDocument;
    private onDidDeleteDocument;
}
